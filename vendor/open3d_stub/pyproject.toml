[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "open3d"
version = "0.0.0"
description = "Stub -- see open3d/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["open3d"]
